# Cloud Infrastructure Components

## 1. Compute Resources

**Resource:** Intel Xeon E312xx, 1 CPU core

**Purpose:** It is used to process commands and run programs on the server.

**Importance:** Compute is needed to run applications and perform different tasks in the cloud.

**KillerCoda:** The server uses an Intel Xeon processor with 1 CPU core. It handles the basic computing tasks of the Linux environment.

## 2. Storage Resources

**Resource:** `/dev/vda1`, 19 GB

**Purpose:** It is used to store files, programs, and the operating system.

**Importance:** Storage is needed to save and access important data in the cloud.

**KillerCoda:** The server has a 19 GB disk with 13 GB available. The main file system is mounted on `/`.

## 3. Networking Resources

**Resource:** `172.30.1.2` and `172.17.0.1`

**Purpose:** It allows the server to communicate with other systems and devices.

**Importance:** Networking allows cloud servers to connect and exchange data with other systems.

**KillerCoda:** The Linux server has two IP addresses, `172.30.1.2` and `172.17.0.1`, which are used for network connectivity.

## 4. Operating System

**Resource:** Ubuntu 24.04.4 LTS

**Purpose:** It manages the server's hardware and provides a platform for running programs and commands.

**Importance:** The operating system is important because it provides the basic environment for managing and using a cloud server.

**KillerCoda:** The server is running Ubuntu 24.04.4 LTS. It is the Linux environment used for the activities in KillerCoda.

